# Core del visor y explorador
pywebview>=6.2.0
send2trash>=2.1.0

# Lectores y transformadores de documentos
mammoth>=1.12.0
openpyxl>=3.1.5
python-pptx>=1.0.2
pypdfium2>=5.13.0
pillow>=12.0.0

# Automatización nativa en Windows (opcional pero recomendado para PPTX COM)
pywin32>=312; sys_platform == 'win32'

# Herramienta de empaquetado y distribución
pyinstaller>=6.22.0
